'use strict'

const cds = require('../cds.js')

describe('DELETE', () => {
  cds.test(__dirname + '/resources')

  describe('from', () => {
    test.skip('missing', () => {
      throw new Error('not supported')
    })
  })

  describe('where', () => {
    test.skip('missing', () => {
      throw new Error('not supported')
    })
  })
})
