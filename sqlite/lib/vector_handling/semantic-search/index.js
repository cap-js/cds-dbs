// Main exports for semantic search functionality
const {
  search,
  embeddings,
  store,
  load,
  register,
  registered,
  loadRegistered,
  clearCache,
  rootDir,
  embeddingsDir
} = './lib/embeddings.js'

module.exports = {
  search,
  embeddings,
  store,
  load,
  register,
  registered,
  loadRegistered,
  clearCache,
  rootDir,
  embeddingsDir
}
